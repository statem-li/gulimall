spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.137.22:3306/gulimall_pms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.137.22:8848
  application:
    name: gulimall-product
  jackson:
    date-format: yyyy-MM-dd
  thymeleaf:
    cache: true
  redis:
    host: 192.168.137.22
    port: 6379

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.atg.gulimall.product: debug
    root: warn
  pattern:
    console: "%d{yyyy-MM-dd} [%thread] - %msg%n"
server:
  port: 8084
gulimall:
  thread:
    core-size: 20
    max-size: 200
    keep-alive-time: 10
